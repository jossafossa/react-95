import { {{pascalCase name}}Section } from "@sections";

type {{pascalCase name}}PageProps = {
};

export const {{pascalCase name}}Page = ({}: {{pascalCase name}}PageProps) => {
  return (
    <>
      <{{pascalCase name}}Section />
    </>
  );
};